# How To Set Up Test Cases

Some test cases will require a manual installation of the launchd configuration files. A helper script (located in `[Build Directory]/library/libxpc/install_launchd_services.sh`) is provided to install the config files into their proper location.
